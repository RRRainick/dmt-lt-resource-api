# 研究内容二：4.4 使用新版代码

源码：D:\Github\dmt-lt。现场副本：D:\Project\resource_review_20260911\resource-api。
旧版备份：同目录 resource-api.before-dmt-lt-20260914。Git 克隆连接失败，源码来自官方提交 ZIP，没有 .git 历史。

## 仅更新模态操作工具（推荐先做）

Windows：
```powershell
scp "D:\Github\dmt-lt\modality_cli.py" "D:\Github\dmt-lt\modality_client.py" root@192.168.4.4:/root/dmt_3/resource-api/
```

4.4 部署 MF，执行一次后退出，模态保留：
```bash
/root/dmt_3/resource-api/.venv/bin/python /root/dmt_3/resource-api/modality_cli.py --ip 192.168.4.16 --port 8021 --node-id node16 --action deploy --modality mf --compute-config-percent 10 --storage-config-mb 128 --forwarding-config-mbps 10 --timeout 30
```

需要删除 MF 时才执行：
```bash
/root/dmt_3/resource-api/.venv/bin/python /root/dmt_3/resource-api/modality_cli.py --ip 192.168.4.16 --port 8021 --node-id node16 --action delete --modality mf --timeout 30
```

沿用原 Python 3.10 虚拟环境；这两个工具仅需标准库，不使用 p4runtime_lib 的 Python 2。node16 沿用现场 HTTP node_id，和 P4Runtime device_id=2 是不同字段。模态名大小写原样发送，此处用 mf。超时后先查看对端状态，不自动重试部署。

## 更新完整研究内容二代码

先在原资源管理器窗口 Ctrl+C 停止进程。上传：
```powershell
scp "D:\Project\resource-api-update.zip" root@192.168.4.4:/root/dmt_3/
```

4.4 备份后覆盖代码，保留已有 .venv：
```bash
cd /root/dmt_3
tar --exclude='resource-api/.venv' -czf "resource-api-before-update-$(date +%Y%m%d-%H%M%S).tar.gz" resource-api
unzip -o /root/dmt_3/resource-api-update.zip -d /root/dmt_3
cd /root/dmt_3/resource-api
RESOURCE_MANAGER_CONFIG=/root/dmt_3/resource-api/config.4.4.json DECISION_ENABLED=false LOG_LEVEL=DEBUG /root/dmt_3/resource-api/.venv/bin/python -u -m resource_manager.run
```

包内现场配置：192.168.4.16:8021，数据库暂用本地文件，课题四资源策略轮询关闭。只运行 modality_cli 不需要启动资源管理器；持续采集资源才运行 resource_manager.run。没有启动4.4上的测试 Agent。

HTTP code=0 表示节点接口返回成功，不等于已验证 P4Runtime 流水线就绪。MF 部署后，可重跑之前的50053端口 P4Info只读查询。若仍报没有流水线配置，需提供方确认初始化流程。
